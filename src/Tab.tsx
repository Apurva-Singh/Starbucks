import React from "react";

interface Props{
 title: string;
 children: React.ReactNode;
}

const Tab: React.FC<Props>=(props)=>{
    return(
        <div>
         
        </div>
    );
};
Tab.defaultProps={
}

export default Tab;